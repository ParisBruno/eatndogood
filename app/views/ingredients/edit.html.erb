<%= render 'shared/page_title', title: t('common.edit_name' , name: "Ingredient Name") %>

<%= render 'shared/errors', obj: @ingredient %>
<div class="row">
  <div class="col-md-12">
    <%= render 'form', remote: false%>
    <div class="colormatch" style="text-align:center">
      [ <%= link_to t('forms.return_to_list'), app_ingredients_path(current_app) %> ]
    </div>
  </div>
</div>