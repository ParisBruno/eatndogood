

<div class="login-header">
  	<h1><%= t('pages.new.edit_welcome_page') %></h1>
	<div class="right-header">
	  <a target="_blank" href="<%= welcome_pages_path(:username=> current_user.full_name,:admin_id => current_user.id) %>" style="float: right;margin-top: -10px;" class="btn btn-success">
	    <%= t('pages.new.save_to_view_changes') %>
	  </a>
	</div>

<hr>

</div>
<div class="row">
	<%= render partial: "form" %>
</div>

