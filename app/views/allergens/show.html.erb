<%= render 'shared/page_title', title: "#{@allergen.name}" %>
<% if @allergen.recipes.count != 0 %>
  
  	<center><h1 align="center">Recipe's containing <%= @allergen.name %></h1></center>
  
  
  <%= render 'recipes/recipe', obj: @allergen %>
  
<% else %>
  <div class="row">
    <h4 align="center">No recipes yet!</h4>
  </div>
<% end %>