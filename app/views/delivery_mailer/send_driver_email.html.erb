<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
    <style>
      table {
        width: 100%;
        border-collapse: collapse;
      }
      th, td {
        border: 1px solid #ddd;
        padding: 8px;
      }
      th {
        background-color: #f2f2f2;
        text-align: left;
      }
    </style>
  </head>
  <body>
    <h1><%= t('delivery.new_delivery_assigned') %></h1>
    <p><%= t('delivery.dear', name: @driver.first_name) %></p>
    <p><%= t('delivery.assigned_following_deliveries') %></p>

    <table>
      <thead>
        <tr>
          <th><%= t('delivery.location') %></th>
        </tr>
      </thead>
      <tbody>
        <% @deliveries.each do |delivery| %>
          <tr>
            <td><%= delivery.location %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </body>
</html>